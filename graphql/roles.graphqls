extend type Query {
    "List all Console roles."
    roles: [RoleName!]!
}

"Role binding type."
type Role {
    "Name of the role."
    name: RoleName!

    "Whether or not the role is global."
    isGlobal: Boolean!

    "Optional target of the role binding."
    targetId: UUID
}